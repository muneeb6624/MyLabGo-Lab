import type { NextConfig } from "next";

const nextConfig: NextConfig = {

    images: {
    domains: ["res.cloudinary.com", ""], // or wherever your Cloudinary images are hosted
  },
  /* config options here */
};

export default nextConfig;
